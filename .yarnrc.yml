nodeLinker: node-modules

# Exact releases admitted before the default age gate: the requested AnimateView release, and the
# SDK's Viewer alpha and Zod Storage extractor this integration needs. Everything else waits.
npmPreapprovedPackages:
  - "motion@13.4.0"
  - "framer-motion@13.4.0"
  - "@transloadit/viewer@0.0.3"
  - "@transloadit/zod@5.0.1"
